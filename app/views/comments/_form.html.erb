<%= form_for([@post, @post.comments.build]) do |f| %>
     <div class="field">
         <%= f.label :comment_body, 'Your Comment here' %><br />
         <%= f.text_area :comment_body %>
     </div>
     <div class="actions">
         <%= f.submit %>
     </div>
 <% end %> 
